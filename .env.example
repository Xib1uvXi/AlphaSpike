# tushare API TOKEN
TUSHARE_TOKEN=your_token

# SQLite PATH
SQLITE_PATH=sqlite_path

# Redis configuration (for sync cache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_password